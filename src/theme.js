import { createTheme } from "@mui/material";

const palette = {
  primary: {
    // main: '#FF0000',
    main: "#3ca832",
  },
  secondary: {
    main: "#FFFF00",
  },
};

const theme = createTheme({
  palette,
});

export default theme;
